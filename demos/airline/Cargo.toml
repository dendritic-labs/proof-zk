[package]
name = "airline-demo"
version = "0.1.0"
edition = "2021"
description = "Airline check-in demo for ProofZK privacy-preserving verification"

[dependencies]
proofzk-core = { path = "../../core" }
tokio = { version = "1.35", features = ["full"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }

[[bin]]
name = "airline-demo"
path = "src/main.rs"